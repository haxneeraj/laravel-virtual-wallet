# laravel-virtual-wallet
Laravel Virtual Wallet is a plug-and-play Laravel package that provides a robust, extendable virtual wallet system for any Laravel application. It's built with flexibility in mind and supports multiple wallet types, transaction logging, and easy integration with existing models like User.
